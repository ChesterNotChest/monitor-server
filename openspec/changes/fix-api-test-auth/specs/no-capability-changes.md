# No Capability Changes

This change is purely test-infrastructure plumbing: wiring the existing `admin_headers` fixture into API test files. No new capabilities, no modified requirements, no API surface changes. All relevant specs (rbac-middleware, test-organization-standard) remain unchanged.
