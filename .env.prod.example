# Monitor Server production environment example.
# Keep real secrets in Jenkins parameters or Jenkins Credentials, not in Git.

APP_DEBUG=false

DATABASE_URL=mysql+pymysql://monitor:monitor_placeholder2026@monitor-mysql:3306/monitor?charset=utf8mb4
JWT_SECRET=change-me-in-jenkins-before-prod
DINGTALK_WEBHOOK=
# DINGTALK_WEBHOOK_URL is also supported for local/manual compose runs.
DINGTALK_WEBHOOK_URL=

HTTP_PORT=8081
RTMP_PORT=1935
STREAM_HTTP_PORT=8082
SRS_API_PORT=1985
SRS_RTC_PORT=8000
SRS_CANDIDATE=10.126.59.25
SRS_PUBLIC_HOST=10.126.59.25

MODEL_DIR=/home/liusu/video/models

YOLO_MODEL_PATH=src/third-party/yolo/yolo11n.pt
WSS_NODE_DEBUG=false
DEBUG_WEB_STREAM=false
DEBUG_FLV_TRANSMIT=false
